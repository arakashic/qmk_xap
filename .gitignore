# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.yarn
target

# Tauri generates a per-OS ACL schema under gen/schemas at build time. Upstream
# tracks the linux/desktop variants; macOS builds additionally emit this one, so
# keep it out instead of committing a per-developer artifact.
src-tauri/gen/schemas/macOS-schema.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# xap-ui build artifacts (generated by xap-ui/build-css.sh)
xap-ui/assets/tailwind.css
xap-ui/bin/
