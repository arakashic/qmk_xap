// One cap size for every keycode-picker path so the catalog grid, the basic
// physical layout, and the MT/QK_MODS hold keys render at the same dimensions
// with the same gap. 54x48 is closest to the KeyCap legend-font tuning (~58px),
// so split/descriptor legends don't clip.
export const PICKER_CAP_W = 54
export const PICKER_CAP_H = 48
export const PICKER_GAP = 5
