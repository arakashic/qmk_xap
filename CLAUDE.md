* use `yarn` instead of `npm`
* XAP protocol is defined in qmk_firmware, see qmk_firmware_ref/ for QMK firmware code
* design docs live in docs/design/ - start with docs/design/architecture.md for the project layout, then the subsystem doc that matches what you're touching
